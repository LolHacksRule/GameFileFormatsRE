//------------------------------------------------
//--- 010 Editor v13.0.1 Binary Template
//
//      File: 
//   Authors: 
//   Version: 
//   Purpose: 
//  Category: 
// File Mask: 
//  ID Bytes: 
//   History: 
//------------------------------------------------

#include "LyN_Common.bt"

struct
{
    LittleEndian();
    uint32 binSize;
    while (FPos != EOF && FTell() != binSize) //hack
    {
        struct
        {
            LyN_BinHeader hdr;
            if (hdr.chunkSz)
            {
                uint32 unk;
                uint32 ver;
                uint32 texts;
                struct
                {
                    uint32 id;
                    uint32 off;
                    float delay;
                    uint32 type;
                    LyN_HandleKey sfx;
                    LyN_HandleKey lip;
                    LyN_HandleKey expr;
                    //FILETIME lud;
                } Text[texts];
                uint32 textSize; //2 * this from here is the offset to the next container.
                byte textChunk[textSize*2];
            }
        } LyN_LocalizedTextContainer;
        FPos = FTell();
    }
} LyN_LocalizedTextContainer_69F1_EA_RGHPC93Bin; //69/F1/RGHPC93,JD4UEA